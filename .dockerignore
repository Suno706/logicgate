__pycache__/
*.pyc
*.pyo
.git/
.gitignore
.vscode/
.idea/
*.log

# Node (built by the frontend-builder stage, not needed in Python stage)
frontend/node_modules/

# Circuit files — persist via volume mount, not baked into the image
circuits/*.json

# ML model pickles — retrained on first boot from CSV
# Comment out to bake pre-trained models into the image
ml_models/saved/

# Dev / test artifacts
tests/__pycache__/
.pytest_cache/
coverage/
*.egg-info/
