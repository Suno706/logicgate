# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.venv/
venv/
env/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Generated / temporary
_check.js
*.log

# Local user data (don't ship saved circuits in the repo)
# Keep circuits/examples/ — that's the curated shared gallery.
circuits/*.json
circuits/s_*/
circuits/user_*/
circuits/u_*/
circuits/room_*/
circuits/g_*/
circuits/default/

# SQLite database (per-deployment user data — never commit)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/user_queries.csv

# Pre-trained models live in ml_models/saved/ and ship via Git LFS so the
# deployed Space boots instantly without retraining on slow 2-vCPU hosts.

# Editor / tooling caches
.claude/

# Test / debug scripts created during dev
_stress_nl.py
_check_seg.py
_trace.py

# pytest
.pytest_cache/
.coverage
htmlcov/

# React / Node (for future frontend/ folder)
node_modules/
dist/
.vite/
*.log

# Environment files (never commit secrets)
.env
.env.local
.env.*.local

# Stray empty files from earlier shell typos
/'
/3
/AND
/dict
/float
/XOR
/(intent_label
/({id
/{i
/r('timeout')
/_single_match_len
/getattr(ml
/dict
/' '
ERR
Together
tau
{n}
